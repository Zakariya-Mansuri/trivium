# Backend API base URL (no trailing slash).
# Local dev default is http://localhost:8000 even without this file.
# On Vercel, set this to your Render URL, e.g. https://trivium-api.onrender.com
VITE_API_URL=http://localhost:8000
